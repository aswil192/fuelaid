<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<meta name="theme-color" content="#27ae60">
	<link href="https://fonts.googleapis.com/css?family=Dosis:200,300,400,500,700" rel="stylesheet">
	<link href="https://fonts.googleapis.com/css?family=Overpass:300,400,400i,600,700" rel="stylesheet">

	<link rel="stylesheet" href="/assets/css/open-iconic-bootstrap.min.css">
	<link rel="stylesheet" href="/assets/css/animate.css">
	<link rel="stylesheet" href="/assets/css/owl.carousel.min.css">
	<link rel="stylesheet" href="/assets/css/owl.theme.default.min.css">
	<link rel="stylesheet" href="/assets/css/magnific-popup.css">
	<link rel="stylesheet" href="/assets/css/aos.css">
	<link rel="stylesheet" href="/assets/css/ionicons.min.css">
	<link rel="stylesheet" href="/assets/css/bootstrap-datepicker.css">
	<link rel="stylesheet" href="/assets/css/jquery.timepicker.css">
	<link rel="stylesheet" href="/assets/css/font-awesome.min.css">
	<link rel="stylesheet" href="/assets/css/flaticon.css">
	<link rel="stylesheet" href="/assets/css/icomoon.css">
	<link rel="stylesheet" href="/assets/css/style.css">
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
	<link rel="stylesheet" href="/assets/css/sidebar.css">
	<link rel="icon" type="image/png" href="/assets/images/FUEL AID_logo.png">

	<title><%= typeof title != 'undefined' ? title : "FuelAid - Fuel Requests" %></title>
</head>

<body>
	<%- include('../partials/header') %>
	<%- include('../partials/alerts') %>

<style>
	/* Header Styling */
	header {
		background: linear-gradient(135deg, #27ae60 0%, #2ecc71 100%)!important;
		box-shadow: 0 4px 15px rgba(0,0,0,0.2)!important;
	}
	header .navbar, .bg-dark, .navbar-dark, .navbar-light {
		background: linear-gradient(135deg, #27ae60 0%, #2ecc71 100%)!important;
		background-image: none !important;
		box-shadow: none !important;
	}
	.logo-container img {
		height: 0.9em;
		width: auto;
	}

	/* Global Animations */
	@keyframes slideInDown {
		from {
			opacity: 0;
			transform: translateY(-30px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@keyframes slideInUp {
		from {
			opacity: 0;
			transform: translateY(30px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@keyframes fadeInScale {
		from {
			opacity: 0;
			transform: scale(0.95);
		}
		to {
			opacity: 1;
			transform: scale(1);
		}
	}

	/* Main Wrapper */
	#main-wrapper {
		overflow-y: scroll;
		background: linear-gradient(135deg, #f5f7fa 0%, #e4e8eb 100%);
		min-height: 100vh;
	}

	#main-wrapper::-webkit-scrollbar {
		display: none;
	}

	/* Page Header */
	.page-header {
		background: linear-gradient(135deg, #27ae60 0%, #2ecc71 100%);
		padding: 25px 30px;
		border-radius: 15px;
		margin: 20px;
		box-shadow: 0 8px 25px rgba(39, 174, 96, 0.3);
		display: flex;
		align-items: center;
		gap: 15px;
		animation: slideInDown 0.6s ease-out;
	}

	.page-header h5 {
		color: white;
		margin: 0;
		font-size: 26px;
		font-weight: 700;
	}

	.page-header i {
		font-size: 28px;
		color: white;
	}

	/* Request Cards Container */
	.requests-container {
		padding: 0 20px 30px;
	}

	/* Request Card */
	.request-card {
		background: white;
		border-radius: 15px;
		box-shadow: 0 8px 25px rgba(0,0,0,0.08);
		margin-bottom: 20px;
		overflow: hidden;
		transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
		animation: fadeInScale 0.6s ease-out;
		border-left: 5px solid #f39c12;
	}

	.request-card:nth-child(1) { animation-delay: 0.1s; }
	.request-card:nth-child(2) { animation-delay: 0.2s; }
	.request-card:nth-child(3) { animation-delay: 0.3s; }
	.request-card:nth-child(4) { animation-delay: 0.4s; }

	.request-card:hover {
		transform: translateY(-5px);
		box-shadow: 0 15px 40px rgba(0,0,0,0.15);
	}

	.request-card-header {
		background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
		padding: 20px 25px;
		display: flex;
		justify-content: space-between;
		align-items: center;
		border-bottom: 1px solid #e9ecef;
	}

	.request-card-header h6 {
		margin: 0;
		font-weight: 700;
		color: #2c3e50;
		font-size: 18px;
		display: flex;
		align-items: center;
		gap: 10px;
	}

	.request-number {
		background: linear-gradient(135deg, #27ae60 0%, #2ecc71 100%);
		color: white;
		width: 35px;
		height: 35px;
		border-radius: 10px;
		display: flex;
		align-items: center;
		justify-content: center;
		font-weight: 700;
	}

	.status-badge {
		padding: 8px 18px;
		border-radius: 25px;
		font-size: 13px;
		font-weight: 600;
		display: flex;
		align-items: center;
		gap: 6px;
	}

	.status-badge.in-progress {
		background: linear-gradient(135deg, #f39c12 0%, #e67e22 100%);
		color: white;
	}

	.request-card-body {
		padding: 25px;
	}

	/* Info Grid */
	.info-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
		gap: 20px;
		margin-bottom: 20px;
	}

	.info-item {
		display: flex;
		align-items: flex-start;
		gap: 12px;
	}

	.info-icon {
		width: 42px;
		height: 42px;
		border-radius: 10px;
		display: flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
	}

	.info-icon i {
		font-size: 18px;
		color: white;
	}

	.info-icon.category { background: linear-gradient(135deg, #3498db 0%, #2980b9 100%); }
	.info-icon.services { background: linear-gradient(135deg, #9b59b6 0%, #8e44ad 100%); }
	.info-icon.customer { background: linear-gradient(135deg, #27ae60 0%, #2ecc71 100%); }
	.info-icon.phone { background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%); }
	.info-icon.location { background: linear-gradient(135deg, #f39c12 0%, #d68910 100%); }
	.info-icon.fuel { background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%); }

	.info-content label {
		display: block;
		font-size: 12px;
		color: #7f8c8d;
		text-transform: uppercase;
		letter-spacing: 0.5px;
		margin-bottom: 3px;
	}

	.info-content span {
		font-size: 15px;
		color: #2c3e50;
		font-weight: 600;
	}

	/* Vehicle Photo */
	.vehicle-photo-wrapper {
		margin-top: 15px;
		padding-top: 15px;
		border-top: 1px dashed #e9ecef;
	}

	.vehicle-photo-wrapper label {
		display: block;
		font-size: 12px;
		color: #7f8c8d;
		text-transform: uppercase;
		letter-spacing: 0.5px;
		margin-bottom: 10px;
	}

	.vehicle-photo {
		max-width: 250px;
		border-radius: 12px;
		border: 3px solid #e9ecef;
		transition: all 0.3s ease;
		cursor: pointer;
	}

	.vehicle-photo:hover {
		transform: scale(1.05);
		box-shadow: 0 10px 30px rgba(0,0,0,0.15);
	}

	/* Action Buttons */
	.action-buttons {
		display: flex;
		gap: 15px;
		margin-top: 20px;
		padding-top: 20px;
		border-top: 1px solid #e9ecef;
	}

	.btn-action {
		padding: 12px 25px;
		border-radius: 10px;
		font-weight: 600;
		font-size: 15px;
		border: none;
		cursor: pointer;
		transition: all 0.3s ease;
		display: flex;
		align-items: center;
		gap: 8px;
	}

	.btn-action.btn-progress {
		background: linear-gradient(135deg, #f39c12 0%, #e67e22 100%);
		color: white;
	}

	.btn-action.btn-progress:hover {
		transform: translateY(-3px);
		box-shadow: 0 8px 20px rgba(243, 156, 18, 0.4);
	}

	.btn-action.btn-complete {
		background: linear-gradient(135deg, #27ae60 0%, #2ecc71 100%);
		color: white;
	}

	.btn-action.btn-complete:hover {
		transform: translateY(-3px);
		box-shadow: 0 8px 20px rgba(39, 174, 96, 0.4);
	}

	/* Empty State */
	.empty-state {
		text-align: center;
		padding: 60px 30px;
		background: white;
		border-radius: 15px;
		box-shadow: 0 8px 25px rgba(0,0,0,0.08);
		animation: fadeInScale 0.6s ease-out;
	}

	.empty-state i {
		font-size: 70px;
		color: #bdc3c7;
		margin-bottom: 20px;
	}

	.empty-state h5 {
		color: #7f8c8d;
		font-weight: 600;
		margin-bottom: 10px;
	}

	.empty-state p {
		color: #95a5a6;
		margin: 0;
	}

	/* Responsive */
	@media (max-width: 768px) {
		.page-header {
			flex-direction: column;
			text-align: center;
			margin: 15px 10px;
		}

		.requests-container {
			padding: 0 10px 20px;
		}

		.request-card-header {
			flex-direction: column;
			gap: 15px;
			text-align: center;
		}

		.info-grid {
			grid-template-columns: 1fr;
		}

		.action-buttons {
			flex-direction: column;
		}

		.btn-action {
			justify-content: center;
		}
	}
</style>

<main>
	<%- include('../partials/fuelSidebar') %>

	<div id="main-wrapper">
		<!-- Page Header -->
		<div class="page-header">
			<i class="fas fa-gas-pump"></i>
			<h5>View Fuel Requests</h5>
		</div>

		<!-- Requests Container -->
		<div class="requests-container">
			<% if (!acceptedServices || acceptedServices.length === 0) { %>
				<div class="empty-state">
					<i class="fas fa-inbox"></i>
					<h5>No Fuel Requests Assigned</h5>
					<p>When admin assigns fuel delivery requests to you, they will appear here.</p>
				</div>
			<% } else { %>
				<% acceptedServices.forEach((service, index) => { %>
					<div class="request-card">
						<div class="request-card-header">
							<h6>
								<span class="request-number"><%= index + 1 %></span>
								Fuel Delivery Request
							</h6>
							<% if (service.status === 'in-progress') { %>
								<span class="status-badge in-progress">
									<i class="fas fa-clock"></i> In Progress
								</span>
							<% } else { %>
								<span class="status-badge">
									<i class="fas fa-hourglass-half"></i> Accepted
								</span>
							<% } %>
						</div>

						<div class="request-card-body">
							<div class="info-grid">
								<div class="info-item">
									<div class="info-icon category">
										<i class="fas fa-gas-pump"></i>
									</div>
									<div class="info-content">
										<label>Service Category</label>
										<span><%= service.serviceCategory || 'Fuel Delivery' %></span>
									</div>
								</div>

								<div class="info-item">
									<div class="info-icon fuel">
										<i class="fas fa-tint"></i>
									</div>
									<div class="info-content">
										<label>Fuel Type</label>
										<span>
											<% if (service.subServices && service.subServices.length > 0) { %>
												<%= service.subServices.join(", ") %>
											<% } else { %>
												Standard Fuel
											<% } %>
										</span>
									</div>
								</div>

								<div class="info-item">
									<div class="info-icon customer">
										<i class="fas fa-user"></i>
									</div>
									<div class="info-content">
										<label>Customer Name</label>
										<span><%= service.customer.firstName %> <%= service.customer.lastName %></span>
									</div>
								</div>

								<div class="info-item">
									<div class="info-icon phone">
										<i class="fas fa-phone-alt"></i>
									</div>
									<div class="info-content">
										<label>Contact Number</label>
										<span><%= service.customer.phone %></span>
									</div>
								</div>

								<div class="info-item">
									<div class="info-icon location">
										<i class="fas fa-map-marker-alt"></i>
									</div>
									<div class="info-content">
										<label>Delivery Location</label>
										<span><%= service.address || 'Address not provided' %></span>
									</div>
								</div>
							</div>

							<% if (service.vehiclePhotoPath) { %>
								<div class="vehicle-photo-wrapper">
									<label><i class="fas fa-camera me-1"></i> Vehicle Photo</label>
									<img src="/<%= service.vehiclePhotoPath %>" alt="Vehicle" class="vehicle-photo">
								</div>
							<% } %>

							<div class="action-buttons">
								<% if (service.status === 'accepted') { %>
								<form action="/fueldeliveryboy/service/inprogress/<%= service._id %>" method="POST" style="display: inline;">
									<button type="submit" class="btn-action btn-progress">
										<i class="fas fa-spinner"></i> Start Delivery
									</button>
								</form>
								<% } %>
							
								<form action="/fueldeliveryboy/service/complete/<%= service._id %>" method="POST" style="display: inline;">
									<button type="submit" class="btn-action btn-complete">
										<i class="fas fa-check-circle"></i> Mark Completed
									</button>
								</form>
							</div>
						</div>
					</div>
				<% }); %>
			<% } %>
		</div>
	</div>
</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="/assets/js/script.js"></script>

</body>
</html>
