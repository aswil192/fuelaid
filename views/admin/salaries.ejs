<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<meta name="theme-color" content="#8e44ad">
	<link href="https://fonts.googleapis.com/css?family=Dosis:200,300,400,500,700" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Overpass:300,400,400i,600,700" rel="stylesheet">

    <link rel="stylesheet" href="/assets/css/open-iconic-bootstrap.min.css">
    <link rel="stylesheet" href="/assets/css/animate.css">
    
    <link rel="stylesheet" href="/assets/css/owl.carousel.min.css">
    <link rel="stylesheet" href="/assets/css/owl.theme.default.min.css">
    <link rel="stylesheet" href="/assets/css/magnific-popup.css">

    <link rel="stylesheet" href="/assets/css/aos.css">

    <link rel="stylesheet" href="/assets/css/ionicons.min.css">

    <link rel="stylesheet" href="/assets/css/bootstrap-datepicker.css">
    <link rel="stylesheet" href="/assets/css/jquery.timepicker.css">

    <link rel="stylesheet" href="/assets/css/font-awesome.min.css">
    
    <link rel="stylesheet" href="/assets/css/flaticon.css">
    <link rel="stylesheet" href="/assets/css/icomoon.css">
	<link rel="stylesheet" href="/assets/css/style.css">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" integrity="sha512-1ycn6IcaQQ40/MKBW2W4Rhis/DbILU74C1vSrLJxCq57o941Ym01SwNsOMqvEBFlcgUa6xLiPY/NS5R+E6ztJQ==" crossorigin="anonymous" referrerpolicy="no-referrer" />
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
	<link rel="stylesheet" href="/assets/css/sidebar.css">
	<link rel="icon" type="image/png" href="/assets/images/FUEL AID_logo.png">
	<title><%= typeof title != 'undefined' ? title : "Salary Management - FuelAid Admin" %></title>
</head>

<body>
	<%- include('../partials/header') %>
	<%- include('../partials/alerts') %>

<style>
	/* Header Styling */
	header {
		background: linear-gradient(135deg, #8e44ad 0%, #9b59b6 100%)!important;
		box-shadow: 0 4px 15px rgba(0,0,0,0.2)!important;
	}
	header .navbar, .bg-dark, .navbar-dark, .navbar-light {
		background: linear-gradient(135deg, #8e44ad 0%, #9b59b6 100%)!important;
		background-image: none !important;
		box-shadow: none !important;
	}
	.logo-container img {
		height: 0.9em;
		width: auto;
	}

	#main-wrapper {
		overflow-y: scroll;
		background: linear-gradient(135deg, #f5f7fa 0%, #e8e8e8 100%);
		min-height: 100vh;
	}

	#main-wrapper::-webkit-scrollbar {
		display: none;
	}

	/* Animations */
	@keyframes slideInDown {
		from {
			opacity: 0;
			transform: translateY(-30px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@keyframes slideInUp {
		from {
			opacity: 0;
			transform: translateY(30px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	/* Page Header */
	.page-header {
		background: linear-gradient(135deg, #8e44ad 0%, #9b59b6 100%);
		padding: 25px 30px;
		border-radius: 15px;
		margin: 20px;
		box-shadow: 0 8px 25px rgba(142, 68, 173, 0.3);
		display: flex;
		align-items: center;
		gap: 15px;
		animation: slideInDown 0.6s ease-out;
	}

	.page-header h5 {
		color: white;
		margin: 0;
		font-size: 26px;
		font-weight: 700;
	}

	.page-header i {
		font-size: 24px;
		color: white;
	}

	/* Stats Cards */
	.stats-row {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 20px;
		padding: 0 20px;
		margin-bottom: 25px;
	}

	.stat-card {
		background: white;
		border-radius: 15px;
		padding: 25px;
		box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
		animation: slideInUp 0.6s ease-out;
		transition: all 0.3s ease;
	}

	.stat-card:hover {
		transform: translateY(-5px);
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.12);
	}

	.stat-card .icon {
		width: 50px;
		height: 50px;
		border-radius: 12px;
		display: flex;
		align-items: center;
		justify-content: center;
		margin-bottom: 15px;
	}

	.stat-card .icon i {
		font-size: 24px;
		color: white;
	}

	.stat-card .value {
		font-size: 28px;
		font-weight: 700;
		color: #2c3e50;
		margin-bottom: 5px;
	}

	.stat-card .label {
		color: #7f8c8d;
		font-size: 14px;
		text-transform: uppercase;
		letter-spacing: 0.5px;
	}

	/* Content Container */
	.content-container {
		padding: 0 20px 30px;
	}

	/* Provider Card */
	.provider-card {
		background: white;
		border-radius: 15px;
		box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
		margin-bottom: 20px;
		overflow: hidden;
		animation: slideInUp 0.6s ease-out;
		transition: all 0.3s ease;
	}

	.provider-card:hover {
		transform: translateY(-3px);
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.12);
	}

	.provider-header {
		padding: 20px 25px;
		border-bottom: 1px solid #e9ecef;
		display: flex;
		justify-content: space-between;
		align-items: center;
	}

	.provider-info {
		display: flex;
		align-items: center;
		gap: 15px;
	}

	.provider-info img {
		width: 60px;
		height: 60px;
		border-radius: 50%;
		object-fit: cover;
		border: 3px solid #8e44ad;
	}

	.provider-info .details h6 {
		margin: 0 0 5px;
		font-size: 18px;
		font-weight: 700;
		color: #2c3e50;
	}

	.provider-info .details p {
		margin: 0;
		color: #666;
		font-size: 14px;
	}

	.role-badge {
		padding: 5px 15px;
		border-radius: 20px;
		font-size: 12px;
		font-weight: 600;
		text-transform: uppercase;
	}

	.role-badge.mechanic {
		background: rgba(30, 60, 114, 0.1);
		color: #1e3c72;
	}

	.role-badge.fueldeliveryboy {
		background: rgba(39, 174, 96, 0.1);
		color: #27ae60;
	}

	.provider-body {
		padding: 20px 25px;
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 20px;
	}

	.earning-stat {
		text-align: center;
		padding: 15px;
		border-radius: 12px;
		background: #f8f9fa;
	}

	.earning-stat .value {
		font-size: 24px;
		font-weight: 700;
		color: #2c3e50;
		margin-bottom: 5px;
	}

	.earning-stat .label {
		font-size: 12px;
		color: #666;
		text-transform: uppercase;
		letter-spacing: 0.5px;
	}

	.earning-stat.pending .value {
		color: #f39c12;
	}

	.earning-stat.total .value {
		color: #27ae60;
	}

	.provider-footer {
		padding: 15px 25px;
		border-top: 1px solid #e9ecef;
		display: flex;
		justify-content: space-between;
		align-items: center;
		background: #f8f9fa;
	}

	.last-paid {
		font-size: 13px;
		color: #666;
	}

	.last-paid i {
		margin-right: 5px;
		color: #8e44ad;
	}

	.btn-payout {
		background: linear-gradient(135deg, #27ae60 0%, #2ecc71 100%);
		color: white;
		border: none;
		padding: 10px 25px;
		border-radius: 8px;
		font-weight: 600;
		cursor: pointer;
		transition: all 0.3s ease;
	}

	.btn-payout:hover {
		transform: translateY(-2px);
		box-shadow: 0 5px 15px rgba(39, 174, 96, 0.3);
		color: white;
	}

	.btn-payout:disabled {
		background: #ccc;
		cursor: not-allowed;
		transform: none;
		box-shadow: none;
	}

	/* Empty State */
	.empty-state {
		text-align: center;
		padding: 60px 20px;
		color: #666;
		background: white;
		border-radius: 15px;
		box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
	}

	.empty-state i {
		font-size: 60px;
		color: #ddd;
		margin-bottom: 20px;
	}

	.empty-state h5 {
		color: #333;
		margin-bottom: 10px;
	}

	/* Modal */
	.modal-content {
		border: none;
		border-radius: 15px;
	}

	.modal-header {
		background: linear-gradient(135deg, #8e44ad 0%, #9b59b6 100%);
		color: white;
		border-radius: 15px 15px 0 0;
	}

	.modal-header .btn-close {
		filter: brightness(0) invert(1);
	}

	/* Responsive */
	@media (max-width: 768px) {
		.page-header {
			margin: 15px 10px;
		}

		.stats-row {
			grid-template-columns: 1fr;
			padding: 0 10px;
		}

		.content-container {
			padding: 0 10px 20px;
		}

		.provider-body {
			grid-template-columns: 1fr;
		}

		.provider-footer {
			flex-direction: column;
			gap: 15px;
		}
	}
</style>

<main>
	<%- include('../partials/adminSidebar') %>
	
	<div id="main-wrapper">
		<!-- Page Header -->
		<div class="page-header">
			<i class="fas fa-money-check-alt"></i>
			<h5>Salary Management</h5>
		</div>

		<!-- Stats Row -->
		<div class="stats-row">
			<div class="stat-card">
				<div class="icon" style="background: linear-gradient(135deg, #f39c12 0%, #e67e22 100%);">
					<i class="fas fa-hourglass-half"></i>
				</div>
				<div class="value">₹<%= totalPendingPayout.toLocaleString('en-IN') %></div>
				<div class="label">Total Pending Payout</div>
			</div>
			<div class="stat-card">
				<div class="icon" style="background: linear-gradient(135deg, #27ae60 0%, #2ecc71 100%);">
					<i class="fas fa-check-circle"></i>
				</div>
				<div class="value">₹<%= totalPaidOut.toLocaleString('en-IN') %></div>
				<div class="label">Total Paid Out</div>
			</div>
		</div>

		<!-- Providers List -->
		<div class="content-container">
			<% if (providers.length === 0) { %>
			<div class="empty-state">
				<i class="fas fa-users"></i>
				<h5>No Service Providers</h5>
				<p>Mechanics and Fuel Delivery Boys will appear here once they register.</p>
			</div>
			<% } else { %>
				<% providers.forEach(provider => { %>
				<div class="provider-card">
					<div class="provider-header">
						<div class="provider-info">
							<img src="<%= provider.profilePic || '/assets/images/default-avatar.png' %>" alt="Provider">
							<div class="details">
								<h6><%= provider.firstName %> <%= provider.lastName %></h6>
								<p><i class="fas fa-phone me-1"></i> <%= provider.phone %></p>
							</div>
						</div>
						<span class="role-badge <%= provider.role %>">
							<%= provider.role === 'mechanic' ? 'Mechanic' : 'Fuel Delivery Boy' %>
						</span>
					</div>
					<div class="provider-body">
						<div class="earning-stat total">
							<div class="value">₹<%= (provider.totalEarnings || 0).toLocaleString('en-IN') %></div>
							<div class="label">Total Earnings</div>
						</div>
						<div class="earning-stat pending">
							<div class="value">₹<%= (provider.pendingEarnings || 0).toLocaleString('en-IN') %></div>
							<div class="label">Pending Payout</div>
						</div>
						<div class="earning-stat">
							<div class="value"><%= provider.commissionRate || 80 %>%</div>
							<div class="label">Commission Rate</div>
						</div>
					</div>
					<div class="provider-footer">
						<div class="last-paid">
							<i class="fas fa-calendar-check"></i>
							Last Paid: <%= provider.lastPaidAt ? new Date(provider.lastPaidAt).toLocaleDateString('en-IN', { dateStyle: 'medium' }) : 'Never' %>
						</div>
						<form action="/admin/payout/<%= provider._id %>" method="POST" style="display: inline;">
							<input type="hidden" name="amount" value="<%= provider.pendingEarnings || 0 %>">
							<button type="submit" class="btn-payout" <%= (provider.pendingEarnings || 0) <= 0 ? 'disabled' : '' %>>
								<i class="fas fa-money-bill-wave me-1"></i>
								Pay ₹<%= (provider.pendingEarnings || 0).toLocaleString('en-IN') %>
							</button>
						</form>
					</div>
				</div>
				<% }) %>
			<% } %>
		</div>
	</div>
</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
<script src="/assets/js/script.js"></script>

</body>
</html>
